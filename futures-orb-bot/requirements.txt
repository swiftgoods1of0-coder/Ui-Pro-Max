# Core dependencies for the Futures Quant Research Platform.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
PyYAML>=6.0
pyarrow>=12.0          # efficient Parquet data cache (falls back to pickle if absent)

# --- Optional / future expansion ---
# scikit-learn>=1.3     # ML signal filters (src/orb_bot/extensions/ml_filter.py)
# pytest>=7.4           # test runner (tests/)
# weasyprint>=61        # preferred PDF engine for the research report
# playwright>=1.40      # PDF fallback (renders the styled HTML via headless
#                       # Chromium); also used to screenshot the dashboard
#                       # if neither is installed, PDF export falls back to a
#                       # dependency-free Matplotlib text rendering
